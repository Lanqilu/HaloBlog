spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://mysql:3306/halo_blog
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto